// with next, no need to import react. it is done automaticaly

import Link from 'next/link';

const Sell = props=> (
    <div>
    <p>pakcspk</p>
    {/* Link allows not to refresh the page */}
    </div>
)

export default Sell ;