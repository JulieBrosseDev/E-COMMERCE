//shortcut to create component with same name as the file : rcc + tab

import React, { Component } from 'react';
import {Query} from 'react-apollo'

class Items extends Component {
    render() {
        return (
            <div>
                <p>Items!</p>
            </div>
        );
    }
}



export default Items;